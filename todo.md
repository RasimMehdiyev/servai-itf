- <input type='checkbox'/> Update RAG in the order overview to work with multi-modal data
- <input type='checkbox'/> Add counterfactual explanations, e.g., "If the confidence score for the item was lower than 75%, the robot would have looked for the item elsewhere".
- <input type='checkbox'/> Attention heatmap/Saliency map: if the robot selects a cereal box, the explanation could show the camera image with a soft highlight around the box and say: “The robot focused mostly on this rectangular package and its label region when deciding it matched the selected item.”

